{
    "name": "App Specific Rules",
    "description": "App Specific ruleset",
    "includedRuleSets": [
        {
            "path": "global.ruleset.json",
            "action": "Default"
        }
    ],
    "rules": [
        {
            "id": "PTE0012",
            "action": "None",
            "justification": "The InternalsVisibleTo property will expose your internal objects to any extension with the given name, publisher, and ID. Using access modifiers for security-sensitive code is not recommended. - App can be used by other SDP apps."
        }
    ]
}

